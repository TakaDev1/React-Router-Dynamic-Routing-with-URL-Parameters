## **課題**

**問題文：**
`/user/:id` にアクセスすると、URLの `id` に対応するユーザー情報（`name` と `email`）を表示してください。
該当するユーザーがいない場合は「User not found」と表示するようにしてください。

**条件：**
(1) `useParams` フックを使用すること
(2) ユーザー情報はハードコーディングで構いません（例：オブジェクト）
(3) TypeScriptの型を適切に付けること
(4) TailwindCSSで見出しに `text-xl font-bold` を適用すること
